name: Publish Button

permissions:
  id-token: write
  contents: read

on:
  push:
    tags:
      - "@vue-nextui/button@*"

jobs:
  button:
    uses: ./.github/workflows/publish.yml
    with:
      component: "button"
