overrides:
  - files:
      - "**/*.{js,mjs,cjs,jsx,vue}"
    options:
      plugins:
        - "@prettier/plugin-oxc"
      parser: oxc
  - files:
      - "**/*.{ts,mts,cts,tsx,vue}"
    options:
      plugins:
        - "@prettier/plugin-oxc"
      parser: oxc-ts
